export = concat;
/**
 * Returns a new Uint8Array created by concatenating the passed ArrayLikes
 *
 * @param {Array<ArrayLike<number>>} arrays
 * @param {Number} length
 * @returns {Uint8Array}
 */
declare function concat(arrays: ArrayLike<number>[], length: number): Uint8Array;
